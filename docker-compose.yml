services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: backend-api
    ports:
      - "${PORT:-1234}:${PORT:-1234}"
    environment:
      PORT: "${PORT:-1234}"
    restart: unless-stopped
